[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"

# Monad Mainnet
[rpc_endpoints]
monad = "${MONAD_RPC_URL}"

[etherscan]
monad = { key = "${EXPLORER_API_KEY}", url = "https://monadexplorer.com/api", chain = 143 }
